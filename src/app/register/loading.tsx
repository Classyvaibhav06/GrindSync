import { SectionLoading } from "@/components/social-grind/SectionLoading";

export default function RegisterLoading() {
  return (
    <SectionLoading
      title="Loading register"
      subtitle="Preparing account setup"
      rows={2}
    />
  );
}
